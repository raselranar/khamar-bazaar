import AddListingPageClient from "./AddListingPageClient";

export const metadata = {
  title: "Add Listing | Khamar Bazaar",
  description: "Publish a livestock or poultry listing to connect with buyers.",
};

export default function AddListingPage() {
  return <AddListingPageClient />;
}
